import {$,show,hide,text} from '../shared/dom.js';function login(){const n=$('studentName').value.trim(),i=$('studentInstituteId').value.trim(),e=$('studentExamId').value.trim();if(!i||!e||!n){text('studentMsg','Institute ID, Exam ID, Name required');return}hide('studentLoginCard');show('studentDashboard');text('studentWelcome',`Welcome ${n}. Institute: ${i}, Exam: ${e}`)}function logout(){show('studentLoginCard');hide('studentDashboard');text('studentMsg','')}$('studentLoginBtn').addEventListener('click',login);$('studentLogoutBtn').addEventListener('click',logout);